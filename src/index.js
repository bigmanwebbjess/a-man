import React from 'react';
import ReactDOM from 'react-dom/client';
import ArmandNemuNature from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ArmandNemuNature />
  </React.StrictMode>
);
